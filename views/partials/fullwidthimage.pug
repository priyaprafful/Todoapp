if (slice.primary.mesh_side)
    -var sectionimage = "image-slice-" + slice.primary.mesh_side
else
    -var sectionimage = "image-slice-left"

div.imagecontent    
    section.content-section(class=sectionimage)
      img.image1(src=slice.primary.background_image.url)
    section.content-section
      img.about-img(src=slice.primary.image.url)